VITE_API_URL=http://127.0.0.1:8000/api
# Set to true to run the SPA against an in-browser mock of the Laravel API
# (useful when the backend is not running). Set to false to hit the real API.
VITE_USE_MOCK=true
